{
  "name": "@giogia/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "gio": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@giogia/core": "workspace:*",
    "@giogia/browser": "workspace:*",
    "@giogia/semantic": "workspace:*",
    "@giogia/ai": "workspace:*",
    "@giogia/runner": "workspace:*",
    "commander": "^13.0.0",
    "chalk": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
